{{define "title"}}Ding Details{{end}}
{{define "content"}}
<article>
  <img src="https://picsum.photos/800/450?random{{.Id}}" alt="">

  <h4>{{.Form.Name}}<sup>{{.Form.Anzahl}}</sup></h4>
  <p>{{.Form.Code}}</p>
  <p>{{.Form.Anzahl}}</p>

  <nav>
    <a href="/dinge/{{.Id}}/edit">Bearbeiten</a>
    <a href="/dinge/{{.Id}}">Einlagern</a>
    <a href="/dinge/{{.Id}}">Entnehmen</a>
  </nav>
</article>
{{end}}
